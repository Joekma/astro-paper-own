---
title: "图标、插画、图片资产：让视觉服务功能而不是抢戏"
author: Joekma
pubDatetime: 2026-05-16T00:00:00.000+08:00
modDatetime: 2026-07-18T00:00:00.000+08:00
slug: ai-cross-platform-design-assets
featured: false
draft: false
tags:
  - 图标设计
  - 视觉设计
  - UI设计
  - 设计资产
description: "讲解业务系统中图标、插画、图片和视觉资产的使用边界，避免装饰抢走功能重点。"
series: AI 多端页面设计
seriesOrder: 22
language: zh-CN
---

## 前置知识与学习目标

**前置知识：** 理解信息层级、颜色语义和可访问性。

**本篇唯一核心问题：** 怎样为图标、插画和图片资产定义功能角色、来源和交付规格？

读完后，你应该能够：能判断何时使用图标、插画或照片，并建立尺寸、格式、替代文本和授权清单。本篇沿用“跨端客户工单管理 SaaS”，核心对象统一为 `ticket`、`customer`、`assignee`、`status`、`priority` 与 `permission`。

上一章已经解决“怎样让微交互解释因果和状态变化，而不是拖慢任务”的问题；本篇把它作为输入，不再重复完整解释。

## 从真实问题出发

工单系统混用线性、填充和 3D 图标，空状态插画很醒目，却没有帮助用户恢复任务。

先不要急着选择组件或颜色。应先写清楚当前用户、对象、触发条件、成功结果和失败后仍需保留的上下文，再判断界面应该表达什么。

## 核心机制

资产首先服务识别、解释或品牌。图标表达紧凑动作，插画帮助解释抽象场景，照片提供真实证据。每项资产需要语义、风格、技术和授权四类元数据。

### 1. 相同动作使用同一图标和标签，危险动作不靠图标颜色单独表达

相同动作使用同一图标和标签，危险动作不靠图标颜色单独表达。它先固定主路径和优先级，后续布局不得反过来改变业务判断。验收时要用真实任务观察用户能否找到下一步。

### 2. 关键图标按钮保留可访问名称，陌生图标优先配文字

关键图标按钮保留可访问名称，陌生图标优先配文字。它把抽象原则转换成可见结构。验收时应检查对象、标签、状态和操作是否逐项对应，而不是凭整体观感打分。

### 3. 装饰图使用空替代文本，信息图提供等价说明

装饰图使用空替代文本，信息图提供等价说明。它负责异常与边界，必须使用空数据、慢请求、权限差异或冲突数据复测，不能只验证理想状态。

### 4. 交付清单记录源文件、导出尺寸、压缩、许可和使用位置

交付清单记录源文件、导出尺寸、压缩、许可和使用位置。它防止局部页面自行发明规则。跨端、跨主题或跨角色检查时，语义保持一致，呈现方式才允许重组。

## 关键角色、数据与状态

| 项目     | 在本篇中的含义                                                                       |
| -------- | ------------------------------------------------------------------------------------ |
| 输入     | 输入是认知目标和显示环境                                                             |
| 业务对象 | `ticket` 是工单，`customer` 是客户，`assignee` 是当前负责人                          |
| 约束     | `permission` 决定可执行动作；`priority` 影响排序，不直接替代业务状态                 |
| 状态主线 | `draft → submitted → processing → resolved → closed`，只有满足权限和前置条件才能转换 |
| 输出     | 是可复用、可追溯的资产库。                                                           |

输入是认知目标和显示环境；中间状态是资产角色与技术规格；输出是可复用、可追溯的资产库。

## 最小实践：贯穿工单案例

“重新分派”使用统一人物切换图标并配文字；无工单空状态使用轻量说明和“调整筛选”按钮，不加入无关人物插画。

执行时使用下面的决策记录，避免示例只有结果而没有中间状态：

```text
输入：角色、ticket、当前 status、permission、终端与网络状态
检查：核心任务、业务规则、可见反馈、失败恢复、跨端差异
中间状态：记录用户动作、系统判断、状态变化和仍被保留的数据
输出：可验证的界面方案、实现约束与验收证据
失败边界：权限不足、空数据、超时、冲突、长文案和窄屏
```

这里的 `status` 表示业务状态；请求的 loading/error 是界面请求状态，两者不能混为一个字段。示例中的数值与规则只用于教学，接入真实项目时必须由产品规则、接口契约和数据字典确认。

## 常见误区

- **为了丰富页面加入无关图标：** 这会让界面结论失去可验证依据；应回到输入、状态和恢复路径重新检查。
- **把文字全部替换为陌生图标：** 这会让界面结论失去可验证依据；应回到输入、状态和恢复路径重新检查。
- **只保存导出 PNG 而丢失来源与许可：** 这会让界面结论失去可验证依据；应回到输入、状态和恢复路径重新检查。

## 适用边界与不适用场景

- 数据与关系图应采用确定性绘制，不交给装饰插画承担。
- 品牌 campaign 资产可有独立风格，但不能混入业务控件图标体系。

如果当前任务没有多对象关系、状态变化或失败恢复，不要为了套用本章而制造额外组件和流程。复杂度必须来自真实认知问题。

## 自检题

1. 用一句话说明：怎样为图标、插画和图片资产定义功能角色、来源和交付规格？
2. 在工单案例中，输入、中间状态和输出分别是什么？
3. 本章方法在哪些场景不应直接套用？

<details>
<summary>查看答案</summary>

1. 资产首先服务识别、解释或品牌。图标表达紧凑动作，插画帮助解释抽象场景，照片提供真实证据。每项资产需要语义、风格、技术和授权四类元数据。
2. 输入是认知目标和显示环境；中间状态是资产角色与技术规格；输出是可复用、可追溯的资产库。
3. 数据与关系图应采用确定性绘制，不交给装饰插画承担。品牌 campaign 资产可有独立风格，但不能混入业务控件图标体系。

</details>

## 本篇总结

能判断何时使用图标、插画或照片，并建立尺寸、格式、替代文本和授权清单。真正的完成标准不是“看起来合理”，而是每条规则都能追溯到任务、数据、状态或规范，并能通过边界场景验证。

## 下一篇衔接

下一篇将解决“怎样用语义 token 重建暗色层级，而不是把亮色页面反相”。本篇输出的决策、约束和案例状态将作为它的直接输入。

## 资料来源

- [Apple HIG：Icons](https://developer.apple.com/design/human-interface-guidelines/icons)
- [WAI：Images Tutorial](https://www.w3.org/WAI/tutorials/images/)
- [Material Design 3：Icons](https://m3.material.io/styles/icons/overview)
